apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
    name: roboshop
    region: us-east-1

managedNodeGroups:
- name: spot
  instanceTypes: ["m5.large","t3.small", "t3.medium"]
  spot: true
  desiredCapacity: 3
